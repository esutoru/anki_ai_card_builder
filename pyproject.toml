[project]
name = "anki-ai-card-builder"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "anki>=25.9.2",
    "aqt>=25.9.2",
]

[dependency-groups]
dev = [
    "mypy>=1.18.2",
]
